---
description: Execute a task using a specified subagent.
argument-hint: Description of the task to execute.
---

$ARGUMENTS

Using a subagent, now think and write elegant code that completes this.

Do not add backwards compatibility unless explicitly requested.

After every bigger change, lint and build.
