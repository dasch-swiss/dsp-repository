name: Docker publish DPE image

on:
  push:
    branches:
      - main
    paths:
      - "dsp-dpe/leptos-dpe/**"
      - "dsp-dpe/Cargo.toml"
      - "dsp-dpe/Cargo.lock"
      - "dsp-dpe/mosaic/tiles/**"

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.run_id }}
  cancel-in-progress: true

jobs:
  compute-tag:
    name: Compute image tag
    runs-on: ubuntu-latest
    outputs:
      tag: ${{ steps.tag.outputs.tag }}
    steps:
      - uses: actions/checkout@v4

      - name: Generate docker image tag
        id: tag
        run: |
          VERSION=$(grep '^version' dsp-dpe/leptos-dpe/server/Cargo.toml | sed 's/.*"\(.*\)"/\1/')
          echo "tag=${VERSION}" >> "$GITHUB_OUTPUT"

  publish:
    name: Build and publish DPE
    needs: compute-tag
    uses: ./.github/workflows/docker-publish.yml
    with:
      dockerfile: dsp-dpe/leptos-dpe/Dockerfile
      image-name: daschswiss/dpe
      image-tag: ${{ needs.compute-tag.outputs.tag }}
    secrets:
      DOCKER_USER: ${{ secrets.DOCKER_USER }}
      DOCKER_HUB_TOKEN: ${{ secrets.DOCKER_HUB_TOKEN }}
