criteria:
  # Kodus does not recognise Playwright E2E tests in a Rust/Leptos monorepo —
  # supplemental assessment script handles e2e-tests.
  # These two criteria genuinely cannot be evaluated from the local filesystem:
  bundle-analysis: false   # N/A — Rust backend, no frontend bundle
  branch-protection: false # Requires GitHub API; checked by assess.py
